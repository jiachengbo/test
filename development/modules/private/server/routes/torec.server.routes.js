"use strict";var path=require("path"),router=require("express").Router(),sequelize=require(path.resolve("./config/lib/sequelize"));module.exports=function(e){if(sequelize.isDefined("Torec")){var r=require("../policies/torec.server.policy"),o=require("../controllers/torec.server.controller");router.route("/").get(r.isAllowed,o.list).post(r.isAllowed,o.create),router.route("/:torecId").get(r.isAllowed,o.read).post(r.isAllowed,o.update).delete(r.isAllowed,o.delete),router.param("torecId",o.torecByID),e.use("/api/torec",router)}};
